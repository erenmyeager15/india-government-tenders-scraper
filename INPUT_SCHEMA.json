{
    "title": "India Government Tenders Scraper",
    "description": "Low-cost sample: collect 1 public GeM tender for the keyword laptop. GeM uses Residential India routing internally; CPPP is guarded because the public listing is CAPTCHA-gated.",
    "type": "object",
    "schemaVersion": 1,
    "required": ["source", "keywords"],
    "properties": {
        "source": {
            "title": "Tender Source",
            "description": "Choose GeM, CPPP, or both. GeM is the reliable default. CPPP is skipped without tender-scraped charges when its public listing shows CAPTCHA.",
            "type": "string",
            "enum": ["gem", "cppp", "both"],
            "default": "gem",
            "editor": "select"
        },
        "keywords": {
            "title": "Search Keywords",
            "description": "Tender search keywords. Up to 5 keywords are processed sequentially to keep runs cost-safe. Start with one keyword.",
            "type": "array",
            "editor": "stringList",
            "maxItems": 5,
            "default": ["laptop"]
        },
        "department": {
            "title": "Department / Ministry",
            "description": "Optional department or ministry text filter.",
            "type": "string",
            "editor": "textfield"
        },
        "state": {
            "title": "State",
            "description": "Optional state filter from enriched GeM detail PDFs when available.",
            "type": "string",
            "editor": "textfield"
        },
        "minValue": {
            "title": "Minimum Tender Value (INR)",
            "description": "Optional minimum tender value. Records with unknown value are excluded when this filter is used.",
            "type": "number",
            "minimum": 0,
            "editor": "number"
        },
        "maxValue": {
            "title": "Maximum Tender Value (INR)",
            "description": "Optional maximum tender value. Records with unknown value are excluded when this filter is used.",
            "type": "number",
            "minimum": 0,
            "editor": "number"
        },
        "dateFrom": {
            "title": "Date From",
            "description": "Optional published-date filter applied after GeM detail PDF enrichment.",
            "type": "string",
            "editor": "textfield"
        },
        "dateTo": {
            "title": "Date To",
            "description": "Optional published-date filter applied after GeM detail PDF enrichment.",
            "type": "string",
            "editor": "textfield"
        },
        "status": {
            "title": "Tender Status",
            "description": "Tender status filter.",
            "type": "string",
            "enum": ["active", "closed", "all"],
            "default": "active",
            "editor": "select"
        },
        "maxResults": {
            "title": "Max Results Per Keyword",
            "description": "Maximum tender records per keyword, up to 50. Start with 1 for a low-cost test.",
            "type": "integer",
            "minimum": 1,
            "maximum": 50,
            "default": 1,
            "editor": "number"
        }
    }
}
